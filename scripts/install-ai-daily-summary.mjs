#!/usr/bin/env node
import {mkdir,writeFile} from 'node:fs/promises';import path from 'node:path';import {homedir} from 'node:os';import {fileURLToPath} from 'node:url';
const label='com.justdeedeemusic.ai-daily-summary';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const escape=s=>s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
const file=path.join(homedir(),'Library/LaunchAgents',label+'.plist');await mkdir(path.dirname(file),{recursive:true});
await writeFile(file,`<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd"><plist version="1.0"><dict><key>Label</key><string>${label}</string><key>ProgramArguments</key><array><string>${escape(process.execPath)}</string><string>${escape(root+'/scripts/jddm-ai-daily-summary.mjs')}</string></array><key>WorkingDirectory</key><string>${escape(root)}</string><key>StartCalendarInterval</key><dict><key>Hour</key><integer>8</integer><key>Minute</key><integer>5</integer></dict><key>StartInterval</key><integer>300</integer><key>RunAtLoad</key><true/><key>StandardOutPath</key><string>${escape(homedir())}/Library/Logs/jddm-ai-daily-summary.log</string><key>StandardErrorPath</key><string>${escape(homedir())}/Library/Logs/jddm-ai-daily-summary.log</string></dict></plist>`);
console.log('Installed local AI recap schedule: 8:05 AM Eastern, with five-minute retry/catch-up.');
