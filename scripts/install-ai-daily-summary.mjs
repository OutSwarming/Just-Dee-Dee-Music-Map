#!/usr/bin/env node
import {mkdir,writeFile} from 'node:fs/promises';import path from 'node:path';import {homedir} from 'node:os';import {fileURLToPath} from 'node:url';
const label='com.justdeedeemusic.ai-daily-summary';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const escape=s=>s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
const intervals=Array.from({length:12},(_,i)=>`<dict><key>Minute</key><integer>${i*5}</integer></dict>`).join('');
const file=path.join(homedir(),'Library/LaunchAgents',label+'.plist');await mkdir(path.dirname(file),{recursive:true});
await writeFile(file,`<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd"><plist version="1.0"><dict><key>Label</key><string>${label}</string><key>ProgramArguments</key><array><string>${escape(process.execPath)}</string><string>${escape(root+'/scripts/jddm-ai-daily-summary.mjs')}</string></array><key>WorkingDirectory</key><string>${escape(root)}</string><key>StartCalendarInterval</key><array>${intervals}</array><key>RunAtLoad</key><true/><key>StandardOutPath</key><string>${escape(homedir())}/Library/Logs/jddm-ai-daily-summary.log</string><key>StandardErrorPath</key><string>${escape(homedir())}/Library/Logs/jddm-ai-daily-summary.log</string></dict></plist>`);
console.log('Installed local AI recap schedule: 6 AM preparation, 7:45 AM refresh, 8:05 AM delivery; five-minute retry/catch-up.');
